runtime: nodejs20
env: standard

env_variables:
  NODE_ENV: "production"

handlers:
  - url: /.*
    script: auto
    secure: always
    redirect_http_response_code: 301

automatic_scaling:
  min_instances: 1
  max_instances: 10
